# Python
backend/.venv/
__pycache__/
*.pyc

# Secrets / local config -- never commit DB credentials
.env
backend/.env

# Local persisted workflow state (SQLite) + admin settings
backend/sop_state.db
*.db
backend/planning_settings.json

# Node
frontend/node_modules/
frontend/dist/

# OS / editor
.DS_Store
.vscode/

# Local AI / agent tooling config (not part of the app)
.claude/
.agents/
.codex/
.mcp.json
skills-lock.json

# ---------------------------------------------------------------------------
# Sensitive business data -- NEVER commit real exports, uploads or dumps
# ---------------------------------------------------------------------------

# Data exports & internal documents (real customer / user / PO / consumption data)
*.xlsx
*.xls
*.csv
*.pdf
*.docx
*.doc

# Data drop folders
PO_receipts/
RM_Consumption/
backend/srdms_uploads/

# Cached pickled data (contains real records)
backend/app/.cache/
*.pkl

# JSON stores with user / role / request / access-log data
# (match anywhere, so copies inside backups/ folders are also ignored)
backend/user_master.json
backend/srdms_store.json
backend/msl_store.json
backend/app_roles.json
user_master.json
srdms_store.json
msl_store.json
app_roles.json
planning_settings.json

# Database dumps and backup archives (SQL dumps + copies of JSON stores)
backups/
backup_data_v1/
z_backup/
z_backup_data_v1/
z_backup*/
*.sql.gz
*.zip
